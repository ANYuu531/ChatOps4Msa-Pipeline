List all microservices in this system. For each service, provide:
(1) service name,
(2) main responsibility in one sentence,
(3) technology stack (language, framework),
(4) the port it listens on if available.
Also indicate whether the service appears in Kubernetes runtime data.

Format as a markdown table:
| Service | Responsibility | Tech Stack | Port | Runtime Detected |

